import './styles.scss';

const Beer = () => {
  console.log('where is the problem (Beer)');
  return (
    <div className="beer">
      <h1 className="beer-title">Beer</h1>
    </div>
  );
};

export default Beer;
