import './styles.scss';

const Meteo = () => {
  console.log('where is the problem (Meteo)');
  return (
    <div className="meteo">
      <h1 className="meteo-title">Meteo</h1>
    </div>
  );
};
export default Meteo;
